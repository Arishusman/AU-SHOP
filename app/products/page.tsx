import {products} from '@/lib/products'; import {ProductCard} from '@/components/ProductCard'; import {SearchBar} from '@/components/SearchBar'; export default function Products(){return <main className="container page"><div className="sectionHead"><div><div className="eyebrow">Catalog</div><h1 style={{fontSize:50}}>All products</h1><p className="muted">{products.length} products in the launch catalog.</p></div></div><SearchBar/><div className="grid">{products.map(p=><ProductCard key={p.id} p={p}/>)}</div></main>}
