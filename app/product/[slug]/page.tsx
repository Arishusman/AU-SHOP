import {products} from '@/lib/products'; import {notFound} from 'next/navigation'; import {ProductActions} from '@/components/ProductActions'; import Link from 'next/link'; import {money} from '@/lib/store'; import type {Metadata} from 'next';
export async function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return{};return{title:p.name,description:`Buy ${p.name} from A.U SHOP. ${p.use}. ${p.benefit}. Available in store.`}}
export default async function Product({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return notFound();const jsonLd={'@context':'https://schema.org','@type':'Product',name:p.name,description:`${p.use}. ${p.benefit}.`,sku:String(p.id),brand:{'@type':'Brand',name:'A.U SHOP'},offers:{'@type':'Offer',priceCurrency:'PKR',price:p.price,availability:p.available?'https://schema.org/InStock':'https://schema.org/OutOfStock',url:`${process.env.NEXT_PUBLIC_SITE_URL||'https://example.com'}/product/${p.slug}`}};return <main className="container page"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><div className="detail"><div className="productArt"><span>{p.name}</span></div><div><div className="eyebrow">In store · Product #{p.id}</div><h1>{p.name}</h1><div className="price" style={{fontSize:28}}>{money(p.price)}</div><p className="muted">{p.use}</p><p style={{fontSize:18,lineHeight:1.7}}>{p.benefit}</p><div className="notice"><b>Availability:</b> {p.available?'Available in store':'Out of stock'} · {p.stock} units</div><ProductActions p={p}/><div className="section"><h3>Product details</h3><p className="muted">Designed for {p.forWho}. Please read the packaging and consult a qualified professional for medicated or health-related products where appropriate.</p></div></div></div><Link href="/products" className="btn ghost">← Back to products</Link></main>}
