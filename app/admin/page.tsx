'use client'; import {useMemo,useState} from 'react'; import {products} from '@/lib/products'; import {categories} from '@/lib/categories'; import {money} from '@/lib/store'; import {Menu,X,Sun,Moon,Package,ShoppingCart,Star,FolderKanban} from 'lucide-react';
export default function Admin(){const[auth,setAuth]=useState(false);const[step,setStep]=useState(0);const[user,setUser]=useState('');const[pass,setPass]=useState('');const[code,setCode]=useState('');const[tab,setTab]=useState('home');const[side,setSide]=useState(false);const[theme,setTheme]=useState('dark');const[prod,setProd]=useState(products);const[cat,setCat]=useState(categories);const login=()=>{if((user==='admin'||user==='arishusm12an@gmail.com')&&pass==='admin123')setStep(1)};if(!auth)return <main className="container page"><div className="summary" style={{maxWidth:460,margin:'70px auto'}}><div className="eyebrow">A.U SHOP ADMIN</div><h1 style={{fontSize:46}}>Dashboard login</h1>{step===0?<div className="form"><input placeholder="Username or admin email" value={user} onChange={e=>setUser(e.target.value)}/><input type="password" placeholder="Password" value={pass} onChange={e=>setPass(e.target.value)}/><button className="btn primary" onClick={login}>Continue</button></div>:<div className="form"><div className="notice">Verification code sent to <b>arishusm12an@gmail.com</b> via Resend after backend configuration. Demo code: any 6 digits.</div><input placeholder="6-digit code" value={code} onChange={e=>setCode(e.target.value)}/><button className="btn primary" onClick={()=>setAuth(true)}>Open dashboard</button></div>}</div></main>;
const nav=[['home','Dashboard Home'],['orders','Orders'],['products','Products'],['categories','Categories'],['reviews','Reviews']];return <div className="adminShell"><header className="adminTop"><button className="iconbtn" onClick={()=>setSide(!side)}>{side?<X/>:<Menu/>}</button><b>A.U SHOP ADMIN</b><div style={{display:'flex',gap:8}}><button className="iconbtn" onClick={()=>{const n=theme==='dark'?'light':'dark';document.documentElement.dataset.theme=n;setTheme(n)}}>{theme==='dark'?<Sun/>:<Moon/>}</button><button className="btn" onClick={()=>setAuth(false)}>Logout</button></div></header><div className="adminLayout"><aside className="adminSide">{nav.map(n=><a href={`#${n[0]}`} key={n[0]} onClick={e=>{e.preventDefault();setTab(n[0])}}>{n[1]}</a>)}</aside>{side&&<div className="drawer" onClick={()=>setSide(false)}><div className="drawerPanel" onClick={e=>e.stopPropagation()}><button className="iconbtn close" onClick={()=>setSide(false)}><X/></button><h2 style={{marginTop:50}}>Admin</h2>{nav.map(n=><a key={n[0]} href="#" onClick={()=>{setTab(n[0]);setSide(false)}}>{n[1]}</a>)}</div></div>}<main className="adminMain">{tab==='home'&&<Home products={prod}/>} {tab==='orders'&&<Orders/>}{tab==='products'&&<Products products={prod} setProducts={setProd}/>} {tab==='categories'&&<CategoriesAdmin categories={cat} setCategories={setCat}/>} {tab==='reviews'&&<Reviews/>}</main></div></div>}
function Home({products}:{products:any[]}){return <><div className="eyebrow">Dashboard Home</div><h1 style={{fontSize:46}}>Overview</h1><div className="stats"><div className="stat"><span className="muted">Total sale</span><h2>Rs 0</h2></div><div className="stat"><span className="muted">Today order</span><h2>0</h2></div><div className="stat"><span className="muted">In shipping</span><h2>0</h2></div><div className="stat"><span className="muted">Delivered orders</span><h2>0</h2></div></div><div className="section"><h2>New orders</h2><div className="empty">No live orders yet. Supabase-backed orders appear here after environment setup.</div></div></>}
function Orders(){const rows=['AU-DEMO-1001','AU-DEMO-1002','AU-DEMO-1003','AU-DEMO-1004','AU-DEMO-1005'];return <><div className="sectionHead"><h1>Orders</h1><span className="muted">Quantity · Product · Buyer · Payment · Delivery · Actions</span></div><div className="table"><table><thead><tr><th>Qty</th><th>Product</th><th>Buyer</th><th>Payment</th><th>Delivery</th><th>•••</th></tr></thead><tbody>{rows.map((r,i)=><tr key={r}><td>1</td><td>Demo order</td><td>Customer {i+1}</td><td>{i%2?'in review':'COD'}</td><td>{i%3===0?'in progress':i%3===1?'in shipping':'delivered'}</td><td><button className="btn" onClick={()=>alert('Actions: change delivery status · change payment status · print invoice · cancel order')}>•••</button></td></tr>)}</tbody></table></div><div className="section"><h2>Cancelled orders</h2><div className="empty">Cancelled orders will appear here.</div></div></>}
function Products({products,setProducts}:{products:any[];setProducts:(x:any[])=>void}){return <><div className="sectionHead"><h1>Products</h1><div><button className="btn primary" onClick={()=>{const n={id:Math.max(...products.map(p=>p.id))+1,slug:'new-product',name:'New Product',price:0,use:'Product use',benefit:'Product benefit',forWho:'All',available:true,stock:10};setProducts([...products,n])}}>Add product</button> <button className="btn" onClick={()=>alert('Select category → search product → tick to add / cross to remove. Backend route included.')}>Add product to category</button></div></div><div className="table"><table><thead><tr><th>Image</th><th>Name</th><th>Price</th><th>Stock</th><th>Actions</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>Product art</td><td>{p.name}</td><td>{money(p.price)}</td><td>{p.stock}</td><td><button className="btn" onClick={()=>{const x=prompt('New price',String(p.price));if(x)setProducts(products.map(q=>q.id===p.id?{...q,price:Number(x)}:q))}}>Edit</button> <button className="btn" onClick={()=>setProducts(products.filter(q=>q.id!==p.id))}>Delete</button></td></tr>)}</tbody></table></div></>}
function CategoriesAdmin({categories,setCategories}:{categories:any[];setCategories:(x:any[])=>void}){return <><div className="sectionHead"><h1>Categories</h1><button className="btn primary" onClick={()=>setCategories([...categories,{id:'new-category',name:'New Category',tagline:'New collection',tone:'rose',subs:['Section 1']}])}>Add category</button></div><div className="grid">{categories.map(c=><div className={`catBanner ${c.tone}`} key={c.id}><div className="catContent"><h3>{c.name}</h3><p>{c.subs.join(' · ')}</p><button className="btn" onClick={()=>setCategories(categories.filter(x=>x.id!==c.id))}>Delete</button></div></div>)}</div></>}
function Reviews(){return <><div className="sectionHead"><h1>Reviews</h1><button className="btn" onClick={()=>alert('Edit/delete review controls are connected to the review API in backend.')}>Manage</button></div><div className="empty">Sample reviews and client reviews are stored in the Supabase reviews table. Admin edit/delete endpoints are included.</div></>}
