'use client'; import {useState} from 'react'; export default function Profile(){const[name,setName]=useState('');const[email,setEmail]=useState('');return <main className="container page"><h1>Profile</h1><div className="summary form"><input placeholder="Full name" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn primary" onClick={()=>alert('Profile saved in demo mode.')}>Save profile</button><button className="btn" onClick={()=>{localStorage.removeItem('au-user');location.href='/account'}}>Logout</button></div></main>}
