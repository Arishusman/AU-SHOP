'use client'; import {useState} from 'react'; export default function Address(){const[v,setV]=useState(()=>typeof window!=='undefined'?localStorage.getItem('au-address')||'':'');return <main className="container page"><h1>Default address</h1><div className="summary form"><textarea rows={6} value={v} onChange={e=>setV(e.target.value)} placeholder="Name, phone, house/street, area, city"/><button className="btn primary" onClick={()=>localStorage.setItem('au-address',v)}>Save address</button></div></main>}
