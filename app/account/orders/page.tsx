'use client'; import Link from 'next/link'; import {useEffect,useState} from 'react'; export default function Orders(){const[o,setO]=useState<any>(null);useEffect(()=>setO(JSON.parse(localStorage.getItem('au-last-order')||'null')),[]);return <main className="container page"><div className="eyebrow">My account</div><h1>Orders</h1><div className="summary">{o?<><div className="row"><b>{o.id}</b><span>{o.status}</span></div><p>{o.items?.map((p:any)=>p.name).join(', ')}</p><Link className="btn primary" href={`/track?order=${o.id}`}>Track</Link></>:<div className="empty">No orders yet.</div>}</div></main>}
