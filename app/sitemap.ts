import type {MetadataRoute} from 'next'; import {products} from '@/lib/products'; import {categories} from '@/lib/categories'; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://example.com';return[{url:base,priority:1},{url:`${base}/products`,priority:.9},{url:`${base}/categories`,priority:.8},...categories.map(c=>({url:`${base}/category/${c.id}`,priority:.7})),{url:`${base}/about`,priority:.6},{url:`${base}/contact`,priority:.6},{url:`${base}/location`,priority:.5},{url:`${base}/privacy-policy`,priority:.6},{url:`${base}/terms`,priority:.5},{url:`${base}/return-refund`,priority:.5},{url:`${base}/shipping`,priority:.5},...products.map(p=>({url:`${base}/product/${p.slug}`,priority:.6,lastModified:new Date()}))]}
