import Link from 'next/link'; import {categories} from '@/lib/categories'; export default function Categories(){return <main className="container page"><div className="eyebrow">Shop by need</div><h1 style={{fontSize:54}}>Categories</h1><div className="grid">{categories.map(c=><Link href={`/category/${c.id}`} key={c.id} className={`catBanner ${c.tone}`} style={{textDecoration:'none',minHeight:230}}><div className="catContent"><h3>{c.name}</h3><div className="muted">{c.subs.join(' · ')}</div></div></Link>)}</div></main>}
