'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {addCart} from '@/lib/store'; export function ProductActions({p}:{p:any}){const[added,setAdded]=useState(false);const router=useRouter();return <div style={{display:'flex',gap:10,marginTop:20,flexWrap:'wrap'}}><button className="btn primary" onClick={()=>router.push(`/checkout?buy=${p.id}`)}>Buy now</button><button className="btn" onClick={()=>{addCart(p);setAdded(true)}}>{added?'Added to cart':'Add to cart'}</button></div>}
