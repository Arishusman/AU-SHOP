'use client'; import Link from 'next/link'; import {ShoppingBag,ArrowRight} from 'lucide-react'; import {addCart,money} from '@/lib/store'; import {useState} from 'react';
export function ProductCard({p}:{p:any}){const[added,setAdded]=useState(false);return <article className="product"><Link href={`/product/${p.slug}`} className="productArt"><span>{p.name}</span></Link><div className="productBody"><div className="pill">IN STORE</div><Link href={`/product/${p.slug}`} className="productName" style={{color:'var(--text)',textDecoration:'none'}}>{p.name}</Link><div className="muted" style={{fontSize:12}}>{p.use}</div><div className="price">{money(p.price)}</div><div className="cardBtns"><button className="btn primary" onClick={()=>{addCart(p);setAdded(true)}}><ShoppingBag size={15}/> {added?'Added':'Add to cart'}</button><Link className="btn ghost" href={`/product/${p.slug}`}>View <ArrowRight size={15}/></Link></div></div></article>}
