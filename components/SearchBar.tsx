'use client'; import {Search,X} from 'lucide-react'; import {useMemo,useState} from 'react'; import Link from 'next/link'; import {products} from '@/lib/products'; export function SearchBar(){const[q,setQ]=useState('');const hits=useMemo(()=>q.trim()?products.filter(p=>p.name.toLowerCase().includes(q.toLowerCase())||p.use.toLowerCase().includes(q.toLowerCase())).slice(0,7):[],[q]);return <div className="searchWrap"><div className="search"><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products, serums, face wash..."/>{q&&<button className="iconbtn" style={{width:34,height:34}} onClick={()=>setQ('')}><X size={16}/></button>}</div>{q&&<div className="suggestions">{hits.length?hits.map(p=><Link key={p.id} href={`/product/${p.slug}`} onClick={()=>setQ('')}>{p.name}<span className="muted" style={{float:'right'}}>Rs {p.price.toLocaleString()}</span></Link>):<div style={{padding:18}} className="muted">No record found</div>}</div>}</div>}
